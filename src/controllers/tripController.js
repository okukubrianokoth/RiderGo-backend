// src/controllers/tripController.js

export const startTrip = (req, res) => {
  res.json({ message: "Trip started (placeholder)" });
};

export const endTrip = (req, res) => {
  res.json({ message: "Trip ended (placeholder)" });
};

export const cancelTrip = (req, res) => {
  res.json({ message: "Trip cancelled (placeholder)" });
};
